name: Pytest Learning

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "My first GitHub Actions job"